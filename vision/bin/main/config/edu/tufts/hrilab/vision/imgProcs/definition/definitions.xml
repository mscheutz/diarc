<!--
Set runtime configuration for a VisionProcessor.

Fields (required unless noted otherwise):

  processor tag:

  	type        : type using an "isA" heirarchy (e.g., color, shape)

	arity       : number of arguments (including variables, descriptors, etc.)

  predicate tag(s):
	
	name        : leading atom's name (e.g., red) and

	data        : (Optional) whatever data is needed for run-time processing

Example: 
              <processor type="color" arity="1">
                <predicate name="red">
                  <r>250</r>
                  <g>0</g>
                  <b>0</b>
                </predicate>
                <predicate name="blue">
                  <r>0</r>
                  <g>0</g>
                  <b>250</b>
                </predicate>
              </processor>

For the definition processor we have:
 varname: the variable name of the predicate, used to designate
					the relation to the definition variables.
 definition: conjunction of terms used to define the definition

 example: "handle(A)" defined as "orange(A),knife(B),partOf(A,B)".
					Here, the variable name "A" links orange and handle, instead
					of handle and knife, for example.

-->
<processor type="type" arity="1">
<!--
  <predicate name="handle">
		<varname>A</varname>
		<definition>
     	<term>orange(A)</term>
     	<term>knife(B)</term>
     	<term>partOf(A,B)</term>
		</definition>
  </predicate>
-->
</processor>
