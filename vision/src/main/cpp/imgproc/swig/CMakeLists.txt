PROJECT(imgproc_swig)

INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})

# set package and outdir
LIST(APPEND options -package ${VISION_BASE_PACKAGE}.imgproc.swig)
SET_SOURCE_FILES_PROPERTIES(ImageProcessor.i PROPERTIES SWIG_FLAGS "${options}")
SET(CMAKE_SWIG_OUTDIR ${SWIG_SRC_OUTDIR}/imgproc/swig)

SET_SOURCE_FILES_PROPERTIES(ImageProcessor.i PROPERTIES CPLUSPLUS ON)
SWIG_ADD_LIBRARY(${PROJECT_NAME} TYPE SHARED LANGUAGE java SOURCES ImageProcessor.i)
