PROJECT(capture_swig)

INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})

# set package and outdir
LIST(APPEND options -package ${VISION_BASE_PACKAGE}.capture.swig)
SET_SOURCE_FILES_PROPERTIES(Capture.i PROPERTIES SWIG_FLAGS "${options}")
SET(CMAKE_SWIG_OUTDIR ${SWIG_SRC_OUTDIR}/capture/swig)

SET_SOURCE_FILES_PROPERTIES(Capture.i PROPERTIES CPLUSPLUS ON)
SWIG_ADD_LIBRARY(${PROJECT_NAME} TYPE SHARED LANGUAGE java SOURCES Capture.i CaptureJNI.cpp)