PROJECT(visionproc_swig)

INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})

# set package and outdir
LIST(APPEND options -package ${VISION_BASE_PACKAGE}.visionproc.swig)
SET_SOURCE_FILES_PROPERTIES(VisionProcess.i PROPERTIES SWIG_FLAGS "${options}")
SET(CMAKE_SWIG_OUTDIR ${SWIG_SRC_OUTDIR}/visionproc/swig)

SET_SOURCE_FILES_PROPERTIES(VisionProcess.i PROPERTIES CPLUSPLUS ON)
SWIG_ADD_LIBRARY(${PROJECT_NAME} TYPE SHARED LANGUAGE java SOURCES VisionProcess.i)
