%% TYPES -- PDDL predefines "object" and "number types"
%subtype(physical, object).
%subtype(abstract, object).

% physical subtypes
subtype(actor, physical).

% abstract subtypes
%subtype(modifier, abstract).