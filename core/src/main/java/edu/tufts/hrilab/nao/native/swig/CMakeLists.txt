PROJECT(FloorSupport_swig)

INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})

SET(CMAKE_SWIG_OUTDIR ${SWIG_SRC_OUTDIR})

LIST(APPEND options -package edu.tufts.hrilab.nao.swig)
SET_SOURCE_FILES_PROPERTIES(FloorSupport.i PROPERTIES SWIG_FLAGS "${options}")
SET_SOURCE_FILES_PROPERTIES(FloorSupport.i PROPERTIES CPLUSPLUS ON)
SWIG_ADD_LIBRARY(${PROJECT_NAME} TYPE SHARED LANGUAGE java SOURCES FloorSupport.i)
